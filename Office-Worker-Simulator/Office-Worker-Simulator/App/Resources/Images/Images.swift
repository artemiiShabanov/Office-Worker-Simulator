import UIKit

enum Images {
    static let oldKey = UIImage(named: "old_key")!
    static let oldKeyboard = UIImage(named: "old_keyboard")!
    static let oldDisplay = UIImage(named: "old_display")!
}
